---
interface Props {
  label?: string;
}
const { label } = Astro.props;
---
<div class="my-5">
  {label && <div class="text-[11px] font-semibold uppercase tracking-wider text-ink-400 mb-2">{label}</div>}
  <div class="formula"><slot /></div>
</div>
